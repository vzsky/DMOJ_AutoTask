// rename this file to config.js and change values where appropiate

module.exports = {
  scp_task_dir: '[user]@[hostname]:[task_lookup_directory]',
  scp_task_statement: '[user]@[hostname]:[task_statements_directory]',
}
